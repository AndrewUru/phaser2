export const BUILD_GRID = {
  minX: -4,
  maxX: 4,
  minY: 0,
  maxY: 11,
  cols: 9,
  rows: 12
};

export const BUILD_LIMITS = {
  maxParts: 24,
  targetStability: 62,
  minimumTwr: 1.02,
  gravityFactor: 12
};

export const BUILD_LABELS = {
  emptySelection: 'Select a placed module to inspect or remove it.',
  emptyValidation: 'Start with a capsule, extend the stack downward, then add thrust and fuel.'
};
